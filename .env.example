# Server (repo-root .env or Vercel project env). Never commit real keys.
RESEND_API_KEY=
# After your Resend domain is verified, use an address on that domain:
# EMAIL_FROM=Aux Party <noreply@yourdomain.com>
EMAIL_FROM=Aux Party <beth.t@example.com>
APP_URL=http://localhost:5173
CORS_ORIGIN=http://localhost:5173
SUPABASE_URL=https://xgypeovqxzazhbucguzt.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
# After schema.sql, also run supabase/schema-profile.sql in the SQL editor (avatars + password reset).

# Vercel sets APP_URL from VERCEL_URL when APP_URL is omitted.
# Same-origin /api on Vercel does not need COOKIE_SAMESITE=none.
