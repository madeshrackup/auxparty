# Leave empty. Local Vite proxies /api to Express; Vercel uses same-origin /api functions.
VITE_API_URL=
# Live rooms need an always-on Node host (not Vercel). Point this at that server.
# VITE_SOCKET_URL=https://game.example.com
